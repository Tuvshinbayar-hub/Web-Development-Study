<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>

<body>
    <form action="/products/{{{product._id}}}?_method=PATCH" method="POST">
        <label for="name">Product Name </label>
        <input type="text" placeholder="Name" id="name" name="name" value="{{product.name}}">
        <br>
        <label for="price">Price</label>
        <input type="number" placeholder="Price in $" name="price" value="{{product.price}}">
        <br>
        <label for="category">Category</label>
        <select name="category" id="category">
            {{#each categories}}
            <option value="{{this}}">{{this}}and{{product.category}}</option>
            {{/each}}
        </select>
        <button>Edit!</button>
    </form>
</body>

</html>